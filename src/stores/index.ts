// Utilities, apuntes: crea y exporta la instancia de Pinia que se usa en toda la aplicación.La exporta como default para que se pueda importar en main.ts o donde se configure la app
import { createPinia } from 'pinia'

export default createPinia()
